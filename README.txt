###My notes###

#Use with texteditor for edit config
~/.gitconfig

#Initialization this repositories
git init

#Clone repository from github
git clone git@github.com:user/repository_name 

#See all avaliable branches
git checkout

#Create new branch
git checkout -b new_branch

#Add files with changes
git add __files or -A for add all files from directories

#Commit in branch
git commit -m "Message commit"

#Push all added changed in project
git push origin branch_name - origin means repositories that were cloned